name: diagnose-indexing-pr

on:
  pull_request:
    branches: [develop]

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  diagnose-server:
    if: github.event.pull_request.head.ref == 'agent/bounded-indexing-pipeline'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "10.x"

      - name: Restore
        run: dotnet restore src/Kioku.Mcp.Server.Tests/Kioku.Mcp.Server.Tests.csproj

      - name: Build tests once
        run: >-
          dotnet build src/Kioku.Mcp.Server.Tests/Kioku.Mcp.Server.Tests.csproj
          --configuration Release
          --no-restore
          --verbosity minimal

      - name: Capture test diagnostics
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          set +e
          dotnet test src/Kioku.Mcp.Server.Tests/Kioku.Mcp.Server.Tests.csproj \
            --configuration Release \
            --no-build \
            --no-restore \
            --verbosity minimal > test-diagnostics.log 2>&1
          status=$?
          if [ "$status" -ne 0 ]; then
            {
              echo '### Focused temporary test diagnostics'
              echo
              echo '```text'
              grep -E -A 14 -B 3 'Failed |Error Message:|Test Run Failed|error CS|\[FAIL\]' test-diagnostics.log | tail -n 180
              echo '```'
            } > test-comment.md
            gh pr comment "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --body-file test-comment.md
          fi
          exit "$status"

      - name: Capture format diagnostics
        if: always()
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          set +e
          dotnet format src/Kioku.Mcp.Server/Kioku.Mcp.Server.csproj \
            --verify-no-changes \
            --verbosity diagnostic > format-diagnostics.log 2>&1
          status=$?
          if [ "$status" -ne 0 ]; then
            {
              echo '### Focused temporary format diagnostics'
              echo
              echo '```text'
              grep -E 'error (WHITESPACE|IDE)|\.cs\(' format-diagnostics.log | tail -n 180
              echo '```'
            } > format-comment.md
            gh pr comment "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --body-file format-comment.md
          fi
          exit "$status"
