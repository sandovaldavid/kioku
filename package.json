{
  "name": "kioku",
  "private": true,
  "license": "MIT",
  "description": "Kioku — MCP Server for Obsidian (the companion Obsidian plugin lives at sandovaldavid/kioku-obsidian)",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=11.0.0"
  },
  "scripts": {
    "prepare": "husky",
    "build:server": "dotnet build src/Kioku.Mcp.Server/Kioku.Mcp.Server.csproj",
    "docs:generate": "node scripts/generate-public-docs.mjs --write",
    "docs:check": "node scripts/generate-public-docs.mjs --check"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.0.0"
  }
}
