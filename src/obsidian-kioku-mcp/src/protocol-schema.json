{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Kioku Bridge Protocol",
  "description": "Shared wire format between the Obsidian plugin bridge and the Kioku MCP server. Since PROTOCOL_VERSION 2, the server sends an 'auth' command with an optional {token} payload as the first message on every (re)connection; the plugin accepts it as a no-op when no authToken setting is configured (backward-compatible with v1 clients that never send it).",
  "definitions": {
    "BridgeMessage": {
      "type": "object",
      "required": ["command"],
      "properties": {
        "command": { "type": "string" },
        "payload": { "type": "object" },
        "requestId": { "type": "string" },
        "protocolVersion": { "type": "integer" }
      },
      "additionalProperties": false
    },
    "BridgeResponse": {
      "type": "object",
      "required": ["success"],
      "properties": {
        "requestId": { "type": "string" },
        "success": { "type": "boolean" },
        "data": {},
        "error": { "type": "string" },
        "protocolVersion": { "type": "integer" }
      },
      "additionalProperties": false
    }
  }
}
