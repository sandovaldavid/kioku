{
  "servers": {
    "kioku": {
      "type": "stdio",
      "command": "dotnet",
      "args": ["run", "--project", "./src/Kioku.Mcp.Server/Kioku.Mcp.Server.csproj"],
      "env": {
        "KIOKU_VAULT_PATH": "${KIOKU_VAULT_PATH}"
      }
    }
  }
}
